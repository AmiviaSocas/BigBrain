from NeuroneMnésique import NeuroneMnésique
from Synapse import Synapse
from FeedbackCircuit import FeedbackCircuit

class Aire29:
    def __init__(self, spatial_memory_neuron_params, synapse_params, feedback_params):
        """
        Modélise l'aire 29, impliquée dans la mémoire épisodique et la navigation spatiale.

        Parameters:
        spatial_memory_neuron_params (dict): Paramètres pour les neurones de mémoire spatiale.
        synapse_params (dict): Paramètres pour les synapses.
        feedback_params (dict): Paramètres pour le circuit de feedback.
        """
        self.neurones_memoire_spatiale = [NeuroneMnésique(**spatial_memory_neuron_params) for _ in range(140)]
        self.synapses = [Synapse(**synapse_params) for _ in range(140)]
        self.feedback_circuit = FeedbackCircuit(**feedback_params)

    def integrer_informations_memoire_et_navigation(self, input_data):
        """
        Simule l'intégration des informations de mémoire et de navigation spatiale dans l'aire 29.

        Parameters:
        input_data (float): Intensité des données de mémoire spatiale.
        Returns:
        dict: Réponses des neurones de mémoire spatiale après traitement.
        """
        reponses = []
        for i, neurone in enumerate(self.neurones_memoire_spatiale):
            neurone.receive_input(input_data * 1.15)  # Modulation pour mémoire et navigation
            response = neurone.activate()
            self.synapses[i].transmit(response)
            reponses.append(response)

        # Ajustement des réponses avec le circuit de feedback
        self.feedback_circuit.apply_feedback(any(reponses))
       
        return {
            "reponses_neurones_memoire_spatiale": reponses,
            "adjustment_feedback": self.feedback_circuit.apply_feedback(True)
        }

# Simulation de l'aire 29
def simulate_aire29():
    # Paramètres ajustables pour l'aire 29
    spatial_memory_neuron_params = {"threshold": 1.0, "sensitivity": 0.85}
    synapse_params = {"weight": 0.5, "neurotransmitter": "glutamate"}
    feedback_params = {"reinforcement_sensitivity": 0.4, "decay_factor": 0.1, "max_strength": 1.6}

    aire29 = Aire29(spatial_memory_neuron_params, synapse_params, feedback_params)

    # Simuler des données pour la mémoire et la navigation
    data_inputs = [1.0, 1.2, 0.9, 1.3, 1.1]  # Liste des données pour la mémoire et la navigation
    for cycle, data in enumerate(data_inputs):
        resultats = aire29.integrer_informations_memoire_et_navigation(data)
        print(f"Cycle {cycle + 1}: Données de mémoire et navigation = {data}, Résultats = {resultats}")

# Exécute la simulation
simulate_aire29()
